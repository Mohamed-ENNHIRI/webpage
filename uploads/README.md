# Images

Photos and figures used on the site. Upload them here, then refer to them from
a Markdown file by their address, which is `/uploads/` followed by the file
name — for example `/uploads/martin-portrait.jpg`.

Keep file names lowercase, with hyphens instead of spaces and no accents.
